
// 29. Write a JavaScript function to get a two digit representation of a year.   

// Examples : 79 or 04

// Test Data :

// dt = new Date(1989, 10, 1);

// console.log(sort_year(dt));

// "89"

function sort_year(tdm) {
    if (sy==tdm) {
       return '"04"';
    }
   }
   let sy = new Date(1989, 10, 1); 
   sy.setFullYear(04);
   console.log(sort_year(sy));